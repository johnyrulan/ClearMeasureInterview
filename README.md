# Clear-Measure Interview Solution
# To generate a build, run the build batch file.
# After running the build script, the resulting dll will appear within the 'build' folder within the root directory.
